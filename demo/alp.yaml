os:
  name: alpine
  image: images:alpine/3.15
include:
- include/device.yaml
stop: true
snapshot: copy

description: |
  creates a simple alpine container that is to be used as a template
  for other containers.  The container is left at a stopped state,
  and a snapshot is made.
