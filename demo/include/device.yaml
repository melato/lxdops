profile-pattern: (instance).demo
device-owner: 1000000:1000000
filesystems:
  main:
    pattern: (demoroot)/(instance)
devices:
  home:
    path: /home
    filesystem: main
  etc:
    path: /etc/opt
    filesystem: main
