profile-pattern: (instance).demo
device-owner: 1000000:1000000
properties:
    section: main
filesystems:
  main:
    pattern: (demoroot)/(section)/(instance)
devices:
  home:
    path: /home
    filesystem: main
  etc:
    path: /etc/opt
    filesystem: main
